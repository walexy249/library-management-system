{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "A simple RESTful API for a Library Management System built with Node.js and Express.js. It allows users to add, borrow, return, and view available books, with MongoDB as the database.",
  "main": "app.js",
  "scripts": {
    "start": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lawal Olawale",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.14.1",
    "morgan": "^1.10.0"
  }
}
